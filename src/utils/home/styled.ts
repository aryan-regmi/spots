import { Button, styled } from '@mui/material';

export const StyledButton = styled(Button)({
    backgroundColor: '#1f1f1f',
    paddingTop: '1em',
    paddingLeft: '1.5em',
    marginBottom: 0,
    paddingBottom: 0,
    cursor: 'pointer',
    userSelect: 'none',
    justifyContent: 'left',
});
