import { Route, Router } from "@solidjs/router";
import "./App.css";

function App() {

  return (
    <main class="container">
      return (
      <main class="container">
        <Router>
          <Route path={'/'} component={<div>Log In</div>} />
        </Router>
      </main>
    </main>
  );
}

export default App;
